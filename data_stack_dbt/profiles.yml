data_days_demo_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password: mysecretpassword
      port: 5432
      dbname: postgres
      schema: modelled